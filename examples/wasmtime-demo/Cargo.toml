[package]
name = "wasmtime-webrtc-host"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
# The `polymorph:webrtc-datachannels` host implementation (data channels
# and the stream/pipe plumbing) lives in this crate. Both binaries obtain their
# `types`/`data-channels` imports from its `add_to_linker`.
wasmtime-webrtc-datachannels = { path = "../../wasmtime-impl" }
# Wasmtime with component-model async (streams/futures) support. The
# `component-model-async` feature gates the `Config::wasm_component_model_async*`
# switches and the host `StreamReader`/`StreamProducer` API used here.
wasmtime = { version = "48", features = ["component-model", "component-model-async", "async", "cranelift", "runtime"] }
wasmtime-wasi = { version = "48", features = ["p3"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread", "sync", "time"] }
futures = "0.3"
anyhow = "1"
env_logger = "0.11.11"
# Native HTTP client backing the echo-remote binary's rendezvous host (plain
# HTTP/1.1 long-poll against the local signaling server; no TLS needed).
reqwest = { version = "0.13", default-features = false }

[dev-dependencies]
# The integration test decodes the guest's base64 SDP blobs to recognize them.
base64 = "0.23"
# The echo-remote integration test starts the signaling mailbox server
# in-process on an ephemeral port.
conformance-signalingd = { path = "../../conformance/signaling/server" }
